# 霧発生装置制御システム（Mist Control System）

このプロジェクトは、PC上のWebブラウザからRaspberry Pi Picoを介して、超音波ミストモジュールを自動制御するシステムです。

## 1. システム構成図
- **ユーザーインターフェース**: Webブラウザ（HTML/CSS/JS）
- **サーバー（制御塔）**: Python (Flask) -> `app.py`
- **通信**: USBシリアル通信 (115200 bps)
- **マイコン（実行役）**: Raspberry Pi Pico (MicroPython) -> `main.py`
- **ハードウェア**: 3.3Vリレー、超音波ミストモジュール、外部電源(5.1V/2.1A)

---

## 2. PC側プログラム: `app.py`
PC上で動作し、ユーザーとの接点（Web画面）と、時間管理、Picoへの命令送信を担当します。

### 主な役割
1. **Webサーバー機能**: 
   - Flaskを使用して、ブラウザに操作画面（UI）を提供します。
2. **時刻監視ロジック（スレッド）**:
   - Web画面を動かしながら、裏側で「現在の時刻」と「予約時刻」を常に比較します。
3. **スヌーズ機能**:
   - 予約時刻になるとPicoに `ON` を送信。指定秒数後に `OFF` を送信します。
   - 「停止ボタン」が押されない限り、15秒の待機を挟んで噴霧を繰り返します。
4. **シリアル通信**:
   - 設定されたポート（例：`COM4`）を通じて、Picoに文字列として命令を送ります。

### 使用ライブラリ
- `flask`: Webサーバー用
- `pyserial`: Picoとの通信用
- `threading`: 時刻監視をバックグラウンドで行うため

---

## 3. Pico側プログラム: `main.py`
Pico内で動作し、PCからの命令を受け取って物理的にリレーを動かします。

### 主な役割
1. **シリアル監視**:
   - USBから送られてくるデータを常に監視します。
2. **リレー制御**:
   - 受信した文字列が `ON` ならGP15ピンをHIGH（3.3V）にし、リレーを閉じます。
   - `OFF` ならLOW（0V）にし、リレーを開きます。
3. **ステータス表示**:
   - Pico本体のLED（25番ピン）をリレーと連動させ、動作状況を目視で確認できるようにしています。

### 動作の流れ
- **待機中**: 無限ループ内で `sys.stdin` を監視。
- **命令受信**: 改行コード（`\n`）が届くまでのデータを読み取り、条件分岐でピンの出力を切り替え。

---

## 4. 接続のポイント（ハードウェア）
安定動作のために以下の配線ルールを適用しています。

- **GND共通化**: 外部電源のマイナス、PicoのGND、ミストのGNDをすべて1箇所に結束。
- **絶縁制御**: Picoとミストの回路をリレーで物理的に分離し、ノイズや逆流を防止。
- **電力供給**: ミストの霧化に必要な大きな電流は、PCからではなく外部ACアダプタから直接供給。

---

## 5. 使い方
1. PicoをPCに接続する。
2. PC側で `python app.py` を実行。
3. ブラウザで `http://localhost:5000` にアクセス。
4. 時刻と噴霧秒数を設定し、「保存」を押して予約完了。